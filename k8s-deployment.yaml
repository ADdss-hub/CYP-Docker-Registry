# CYP-Docker-Registry Kubernetes Deployment
# Version: v1.0.7
# 零信任容器镜像私有仓库 K8s 部署配置
---
apiVersion: v1
kind: Namespace
metadata:
  name: cyp-docker-registry
  labels:
    app: cyp-docker-registry
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: cyp-docker-registry-config
  namespace: cyp-docker-registry
data:
  config.yaml: |
    server:
      host: "0.0.0.0"
      port: 8080
      mode: "release"
    
    storage:
      blob_path: "/data/blobs"
      meta_path: "/data/meta"
      cache_path: "/data/cache"
      max_cache_size: "10GB"
    
    security:
      force_login: true
      max_login_attempts: 3
      lock_on_intrusion: true
      audit_blockchain: true
      jwt_secret: "${JWT_SECRET}"
    
    accelerator:
      enabled: true
      upstreams:
        - name: "docker-hub"
          url: "https://registry-1.docker.io"
          priority: 1
        - name: "gcr"
          url: "https://gcr.io"
          priority: 2
    
    signature:
      enabled: true
      mode: "warn"
      key_path: "/data/signatures"
    
    sbom:
      enabled: true
      generator: "syft"
      format: "spdx-json"
      storage_path: "/data/sboms"
      vuln_scan: true
---
apiVersion: v1
kind: Secret
metadata:
  name: cyp-docker-registry-secrets
  namespace: cyp-docker-registry
type: Opaque
stringData:
  jwt-secret: "change-me-in-production-use-strong-secret"
  admin-password: "admin123"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cyp-docker-registry-data
  namespace: cyp-docker-registry
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: standard
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyp-docker-registry
  namespace: cyp-docker-registry
  labels:
    app: cyp-docker-registry
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cyp-docker-registry
  template:
    metadata:
      labels:
        app: cyp-docker-registry
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: cyp-docker-registry
          image: cyp-docker-registry:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: cyp-docker-registry-secrets
                  key: jwt-secret
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: cyp-docker-registry-secrets
                  key: admin-password
          volumeMounts:
            - name: config
              mountPath: /app/configs
              readOnly: true
            - name: data
              mountPath: /data
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      volumes:
        - name: config
          configMap:
            name: cyp-docker-registry-config
        - name: data
          persistentVolumeClaim:
            claimName: cyp-docker-registry-data
---
apiVersion: v1
kind: Service
metadata:
  name: cyp-docker-registry
  namespace: cyp-docker-registry
  labels:
    app: cyp-docker-registry
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: cyp-docker-registry
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cyp-docker-registry
  namespace: cyp-docker-registry
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - registry.example.com
      secretName: cyp-docker-registry-tls
  rules:
    - host: registry.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cyp-docker-registry
                port:
                  name: http
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cyp-docker-registry
  namespace: cyp-docker-registry
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: cyp-docker-registry
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cyp-docker-registry
  namespace: cyp-docker-registry
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cyp-docker-registry
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: cyp-docker-registry
  namespace: cyp-docker-registry
spec:
  podSelector:
    matchLabels:
      app: cyp-docker-registry
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
  egress:
    - to:
        - ipBlock:
            cidr: 0.0.0.0/0
      ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 80
